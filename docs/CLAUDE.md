# docs/
Canonical product and handoff documentation for Takdizang.

## Structure
- `status/`: current state, validation logs, operator handoff
- `ref/`: slower-changing architecture and contract docs

## Convention
- Keep `status/` short and current.
- Put route/schema/storage details in `ref/`.
